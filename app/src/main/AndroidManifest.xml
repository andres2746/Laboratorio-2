<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sistema_login">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic__048314"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic__048314"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sistema_Login">
        <activity
            android:name=".menu"
            android:label="Bienvenido"></activity>
        <activity android:name=".registros"
                  android:label="Registrese"/>
        <activity
            android:name=".Splash"
            android:label="Inicio de sesion" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>